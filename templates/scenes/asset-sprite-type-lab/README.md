# Asset Sprite Type Lab

Demonstrates template-scoped assets:

- Font assets disclosed in the manifest and loaded by Studio/Showcase.
- Image sprite assets resolved through `asset("...")`.
- Font weight animation over time.
- Sprite motion that stays declarative and reusable.

Public builds can replace `localPath` with Bunny CDN `publicUrl` values without changing the TSX.
