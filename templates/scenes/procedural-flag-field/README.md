# Procedural Flag Field

Demonstrates `Procedural.Visual` with a bounded render function that produces many 2D draw commands from loops, deterministic random values, noise helpers, settings, and derived tokens.

This template intentionally avoids a bespoke preset. The waving fabric tiles, thread highlights, and particle field are all generated inside the procedural render callback.
