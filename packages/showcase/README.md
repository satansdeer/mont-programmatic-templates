# Showcase

Static public gallery for approved community templates.

The production showcase should publish only templates with:

- `reviewStatus: "approved"`
- `ipRisk: "generic"`
- `publishToShowcase: true`
